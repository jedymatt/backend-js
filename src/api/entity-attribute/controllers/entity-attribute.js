'use strict';

/**
 *  entity-attribute controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::entity-attribute.entity-attribute');
