'use strict';

/**
 *  character-attribute controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::character-attribute.character-attribute');
